<Page x:Class="Individual_project_initial.Reminders"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Individual_project_initial"
      mc:Ignorable="d" 
      d:DesignHeight="720" d:DesignWidth="1280"
      Title="Reminders">
    <Viewbox Stretch="Uniform">
        <Grid Height="720" Width="1280" Background="White">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Text="Set a Reminder" FontSize="30" HorizontalAlignment="Left" Margin="120,10,0,0" TextWrapping="Wrap" Height="39" Width="355"/>
            <Separator Grid.Row="1" Margin="120,0,0,0" Height="15" Width="355" HorizontalAlignment="Left"/>
            <RichTextBox Grid.Row="2" Name="ReminderNote" Height="80" Margin="120,0,890,0" VerticalAlignment="Center">
                <FlowDocument>
                    <Paragraph>
                        <Run Text="Reminder Note"/>
                    </Paragraph>
                </FlowDocument>
            </RichTextBox>
            <Label Grid.Row="3" Content="Reminder Date" HorizontalAlignment="Left" Margin="120,0,0,0" VerticalAlignment="Center" Width="150"/>
            <DatePicker Grid.Row="4" Name="ReminderDate" Margin="120,0,1010,0" VerticalAlignment="Center"/>
            <Button Grid.Row="5" Content="Set Reminder" Name="ReminderSubmit" Click="SubmitButton_Click" HorizontalAlignment="Left" Margin="120,10,0,488" Width="100" Grid.RowSpan="2"/>
        </Grid>
    </Viewbox>
</Page>
