<Page x:Class="Individual_project_initial.Accounts"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Individual_project_initial"
      mc:Ignorable="d" 
      d:DesignHeight="720" d:DesignWidth="1280"
      Title="Accounts">
    <Viewbox Stretch="Uniform">
        <Grid Height="720" Width="1280" Background="White">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" FontSize="30" HorizontalAlignment="Left" Margin="120,10,0,0"
                       TextWrapping="Wrap" VerticalAlignment="Top" Height="39" Width="300">
                <Run Language="en-gb" Text="Accounts"/>
            </TextBlock>
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="120,10,0,0">
                <Button Content="Add Account" Click="Button_Click_1" Margin="0,0,10,0"/>
                <Button Content="Add Transaction" Click="Button_Click"/>
            </StackPanel>
            <Separator Grid.Row="2" Margin="120,10,0,0" Height="15" Width="355" HorizontalAlignment="Left"/>
            <ScrollViewer Grid.Row="3" Margin="120,10,120,10" HorizontalAlignment="Left" Width="1039">
                <StackPanel x:Name="AccountStackPanel" Width="1019"/>
            </ScrollViewer>
        </Grid>
    </Viewbox>
</Page>